import LoadMoreShips from '@/components/LoadMoreShips.vue'

describe('Mounting Component', () => {
  test('mount component with values from the store', async () => {
      expect(LoadMoreShips).toBeTruthy();
  })
})
